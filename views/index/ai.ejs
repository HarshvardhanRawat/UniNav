<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniNav - AI Assistant</title>
    <link rel="stylesheet" href="/css/styleai.css">
    <link rel="icon" href="/images/logo.png" type="image/png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
</head>
<body>
    <div class="ai-wrapper">
        <div class="ai-container">
            <!-- Header Section -->
            <div class="chat-header">
                <div class="header-icon">🤖</div>
                <h1>Hey there! 👋</h1>
                <p class="header-subtitle">I'm your friendly campus guide. Where would you like to go today?</p>
            </div>

            <!-- Messages Area -->
            <div class="messages-area" id="messagesArea">
                <!-- Welcome Message -->
                <div class="message bot-message">
                    <div class="message-avatar">🎯</div>
                    <div class="message-bubble bot-bubble">
                        <p>Hi! I'm here to help you navigate campus with ease! 🗺️</p>
                        <p class="message-hint">Ask me anything like "Where is room 203?" or "How do I get to the library?"</p>
                    </div>
                </div>

                <% if (answer) { %>
                <!-- User Question (if we had it, would show here) -->
                <!-- Bot Answer -->
                <div class="message bot-message">
                    <div class="message-avatar">🤖</div>
                    <div class="message-bubble bot-bubble">
                        <div class="answer-content"><%= answer %></div>
                    </div>
                </div>
                <% } %>
            </div>

            <!-- Input Section -->
            <div class="input-section">
                <form method="POST" action="/ai/ask" id="aiForm" class="chat-form">
                    <div class="input-wrapper">
                        <input 
                            type="text" 
                            name="question" 
                            id="questionInput"
                            placeholder="Ask me anything about campus locations..." 
                            required 
                            autocomplete="off"
                        />
                        <button type="submit" class="send-button" id="sendButton">
                            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                                <path d="M22 2L11 13" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                                <path d="M22 2L15 22L11 13L2 9L22 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                            </svg>
                        </button>
                    </div>
                </form>
                <p class="input-hint">💡 Tip: Press Enter to send</p>
            </div>
        </div>
    </div>

        <script type="module" src="/js/ai-interactions.js"></script>
    
</body>
</html>